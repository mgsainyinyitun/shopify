import { Box, Typography } from "@mui/material";

export default function LinkedBankList(){
    return(
        <Box m={1} sx={{display:'flex',justifyContent:'center'}}>
            <Typography variant="caption">No Information</Typography>
        </Box>
    );
}